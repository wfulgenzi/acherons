# `yarn dev` / `yarn build:dev` → `--mode development` → `.env.development` (localhost).
# `yarn build`                  → `--mode production`   → `.env.production` (ship URL).
#
# Override without editing committed files: use `.env.development.local` or
# `.env.production.local` (gitignored at repo root).
